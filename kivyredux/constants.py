class StoreProps(object):
    mapping = "map"
    binding = "bind"
    map_cb= "cb"
    bind_prop = "prop"
    bind_dispatch = "dispatch"
    bind_bounded="binded"

class ConnectionProps(object):
    bind='bind'
    init='init'